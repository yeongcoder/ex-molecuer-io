
module.exports = {
	// Namespace of nodes to segment your nodes on the same network.
	namespace: "ex-moleculer-js",

	// Called after broker created.
	created(_broker) {
	},
	// Called after broker started.
	started(_broker) {
	},

	// Called after broker stopped.
	stopped(_broker) {
	}
};
